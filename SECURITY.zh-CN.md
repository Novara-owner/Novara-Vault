# Novara 安全说明

<p align="center"><a href="SECURITY.md">English</a> · <strong>简体中文</strong></p>

---

> 本文档有两个用途：(1) 告诉安全研究人员**如何私下报告漏洞**；(2) 向用户说明 **Novara 如何保护你的数据、以及不保护什么**。
>
> **生效日期：** 2026-08-14 · **最近更新：** 2026-08-17 · **适用范围：** Novara 4.0（标注处亦覆盖更早版本）

---

## 1. 受支持的版本

以下版本提供安全修复。我们强烈建议始终运行最新版本。

| 版本 | 状态 | 说明 |
|------|------|------|
| 4.0 | ✅ 支持 | 当前版本 |
| 3.0 | ✅ 支持 | 在可行范围内提供关键修复 |
| 2.0 | ⚠️ 旧版 | 使用较旧的 AES-CBC 加密；**建议升级**（见第 7 节） |
| < 2.0 | ❌ 不支持 | |

## 2. 报告漏洞

我们认真对待安全报告，且绝不惩罚善意的安全研究。

**请先私下报告**——不要在公开 Issue 中贴出漏洞细节。

- **首选：** GitHub → **Security** 标签 → **Report a vulnerability**（私密漏洞上报）。
- **备选：** 发送邮件至 **owner@novara.xin**，标题以 `[Security] ...` 开头。

请尽量包含：

1. 受影响的版本；
2. 对问题及其潜在影响的清晰描述；
3. 复现步骤（或概念验证），如有；
4. 建议的修复方案（可选）。

**你可以期待：**

- 我们力争在 **5 个工作日内**确认收到你的报告。
- 我们会持续告知你评估与修复进展。
- **高危漏洞在修复发布前不会公开披露。** 低危问题将与你协商处理。
- 致谢：我们乐于在发布说明中致谢报告者，除非你要求匿名。

---

## 3. 安全模型与威胁模型

**我们保护什么。** Novara 是一款 *本地优先* 应用。其安全目标是保护**静止数据**（磁盘上的单文件数据库），使其免于：

- 在应用锁定或关闭时被随意或未经授权地读取（通过可选加密）；
- 加密文件被静默篡改（通过 AES-GCM 认证）；
- 意外数据丢失（通过原子写入、回收站与本地备份）；
- 锁屏密码被暴力猜解（通过锁定机制与单调时钟）。

**我们不保护什么（诚实的边界）。** 本地应用无法抵御一切。Novara **不**抵御：

- **已解锁的会话**——应用解锁运行期间，能操作你电脑的人能看到你所看到的内容。
- **你电脑上的恶意软件**——键盘记录、屏幕录制、远程木马，或已被攻陷的 Windows 账户，无论 Novara 是否加密，都可能窥探或窃取数据。
- **物理接触**——能物理接触你已解锁设备、或能重置 Windows 密码的人，可能触及你的文件。
- **丢失/遗忘密码**——设计上无恢复机制。没有后门的加密，同时意味着没有密码重置。
- **内存提取**——应用解锁期间加密密钥驻留内存；具备相应工具的高水平本地攻击者可能将其提取。

> **结论：** Novara 的加密保护的是**磁盘上的静止数据**。它不能替代一台健康、无恶意软件、物理环境安全的电脑。

## 4. 加密体系

开启隐私锁后，Novara 对整个数据库加密：

| 项目 | 详情 |
|------|------|
| 加密算法 | AES-256-GCM（认证加密），256 位密钥 |
| 密钥派生 | PBKDF2-SHA256，100,000 次迭代，每用户随机 32 字节盐 |
| 密码 | 6–64 位字符；本地仅存加盐 SHA-256 哈希（绝不存明文） |
| 认证 | 12 字节随机 nonce + 16 字节 GCM tag——任何篡改都会被检出 |
| 数据流 | JSON → GZip 压缩 → AES 加密 → 落盘文件 |
| 文件格式 | 版本化头（`NOVA` 魔数 + 版本 + 加密标记 + 16 字节完整性字段）+ 加密体 |
| 迁移 | 旧版 v1（AES-CBC，Novara 2.0）格式只读，并在一次用户确认后自动迁移到 v2（GCM） |

**重要：** 加密**默认关闭**。未开启隐私锁时，数据库是明文 JSON 文件（仅受 Windows 账户权限保护）。这是刻意的设计选择，避免普通用户被自己的数据锁在门外。

## 5. 数据完整性与可靠性

Novara 内置多层机制防止数据丢失与损坏：

- **原子写入**——每次保存先写临时文件、刷盘（`Flush(true)`）、再原子替换原文件。写入中途崩溃不会损坏数据库。
- **单写者纪律**——去抖/串行化的保存队列 + 单实例互斥，防止并发写入互相覆盖。
- **独占文件锁 + Hidden/ReadOnly 属性**——降低第三方软件误改数据库的概率。
- **回收站（软删除）**——删除的卡片 7 天内可恢复，超期后下次启动永久清理。
- **本地滚动备份**——内置备份保留最多 10 份可恢复的本地快照（4.0+）。
- **崩溃日志（本地、脱敏）**——崩溃详情本地落盘（4.0+）供日后排查；敏感值（如 API Key）已脱敏，且绝不自动上传。
- **完整性校验**——导出格式带 16 字节 MD5 头；导入经校验、归一化，失败即回滚。

## 6. 防暴力破解与锁定

- **连续 5 次输错密码**后，锁定 30 分钟。
- 失败次数与锁定截止时间跨重启持久化（`lockout.dat`），重启程序不会重置锁定。
- 自 4.0 起，锁定使用**单调时钟**（`Environment.TickCount64`），防止通过回拨系统时间绕过。
- 锁屏在窗口失焦时清空输入框，降低旁人窥视风险。

## 7. 版本兼容与降级警告

| 场景 | 行为 |
|------|------|
| 在 3.0/4.0 中打开 2.0 数据 | ✅ 可读，并在一次确认后从 AES-CBC（v1）迁移到 AES-GCM（v2） |
| 在 2.0 中打开 3.0/4.0 数据 | ❌ **无法读取**——2.0 不识别 GCM 格式 |
| 3.0 ↔ 4.0 | ✅ 同为 v2 GCM 格式，兼容 |

> ⚠️ **降级或回滚到 2.0 之前**，请先导出一份明文备份。数据库一旦迁移到 GCM，旧版本将无法打开。

## 8. 应用加固

- **日记编辑器 XSS 防护**——富文本 HTML 在加载、保存与导航后均经过严格标签/属性/URL 白名单净化，使用真实 HTML 解析器（AngleSharp）。脚本标签、`on*` 事件属性（含 `o&#110;load` 之类的实体编码变体）、危险协议（`javascript:`、`vbscript:`、非图片 `data:`）均被剥离。标题以纯文本渲染。
- **不执行不可信输入**——导入的 HTML 与 JSON 只解析与归一化，绝不执行。
- **仅本地的辅助进程**——桌面便签辅助进程通过本机文件与命名事件与主程序通信，不产生任何网络请求，也不开放任何监听端口。
- **最小攻击面**——不监听网络端口、无 HTTP 服务器、不向网络暴露任何远程调用接口。整个应用中唯一的出站网络调用，是用户触发的 API Key 连通性测试（见隐私政策）。

## 9. 开发者承诺

作为项目所有者，我承诺：

1. **绝不**运营收集用户数据的云服务器。
2. **绝不**加入后门、密码找回绕过机制，或任何远程解锁/窃取数据的途径。
3. **绝不**加入遥测、分析或广告。
4. **绝不**在未经用户显式操作的情况下自动更新或自动上传数据。
5. **透明披露**安全问题：先修复、后发布，并致谢报告者。

## 10. 未来变更

下一个大版本（5.0）计划加入本地 Agent/MCP 接口，让 AI 助手可以代你读写卡片。该功能上线时，本文档将新增专门章节，说明其权限模型、鉴权与数据边界。核心原则——Novara 自身绝不传输你的数据——将保持不变。

## 11. 联系方式

- **邮箱：** owner@novara.xin
- **GitHub：** https://github.com/Novara-owner/Novara-Vault
