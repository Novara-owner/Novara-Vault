<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="Novara.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Novara"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Novara"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource AppSurfaceBrush}">

        <Grid.RowDefinitions>
            <RowDefinition Height="36" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="CustomTitleBar" Background="Transparent">
            <TextBlock
                Text="Novara"
                FontFamily="Century Gothic"
                VerticalAlignment="Center"
                Margin="16,0,0,0"
                FontSize="15"
                Foreground="{ThemeResource AppPrimaryButtonBrush}" />
        </Grid>

        <Grid x:Name="NavBar" Grid.Row="1" Margin="60,4,60,8" Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" x:Name="NavMemo" Style="{StaticResource NovaraNavButtonStyle}" Click="NavButton_Click" Tag="Memo" Margin="0,0,10,0" IsTabStop="False" ToolTipService.ToolTip="Ctrl+1">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Path x:Name="NavMemoPath" Width="20" Height="20" Stretch="Uniform"
                          Fill="{ThemeResource IconForegroundBrush}" />
                    <TextBlock x:Name="NavMemoText" Text="备忘" Tag="Nav_Tab_Memo" VerticalAlignment="Center" FontSize="14" />
                </StackPanel>
            </Button>
            <Button Grid.Column="1" x:Name="NavFile" Style="{StaticResource NovaraNavButtonStyle}" Click="NavButton_Click" Tag="File" Margin="0,0,10,0" IsTabStop="False" ToolTipService.ToolTip="Ctrl+2">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Path x:Name="NavFilePath" Width="20" Height="20" Stretch="Uniform"
                          Fill="{ThemeResource IconForegroundBrush}" />
                    <TextBlock x:Name="NavFileText" Text="文件" Tag="Nav_Tab_File" VerticalAlignment="Center" FontSize="14" />
                </StackPanel>
            </Button>
            <Grid x:Name="NavPlanHost" Grid.Column="2" Margin="0,0,10,0">
                <Button x:Name="NavPlan" Style="{StaticResource NovaraNavButtonStyle}" Click="NavButton_Click" Tag="Plan" IsTabStop="False" ToolTipService.ToolTip="Ctrl+3">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Path x:Name="NavPlanPath" Width="20" Height="20" Stretch="Uniform"
                              Fill="{ThemeResource IconForegroundBrush}" />
                        <TextBlock x:Name="NavPlanText" Text="计划" Tag="Nav_Tab_Plan" VerticalAlignment="Center" FontSize="14" />
                    </StackPanel>
                </Button>
                <Button x:Name="NavPlanFilterButton"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Margin="0,0,10,0"
                        Width="28" Height="28"
                        Background="Transparent"
                        BorderThickness="0"
                        CornerRadius="8"
                        Padding="0"
                        Visibility="Collapsed"
                        IsTabStop="False"
                        Click="NavPlanFilterButton_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Grid x:Name="Root" Background="{TemplateBinding Background}" CornerRadius="{TemplateBinding CornerRadius}">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Root" Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="#33FFFFFF" />
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Root" Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="#59FFFFFF" />
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <ContentPresenter Content="{TemplateBinding Content}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Grid>
                        </ControlTemplate>
                    </Button.Template>
                    <Viewbox Width="14" Height="14" Stretch="Uniform">
                        <PathIcon x:Name="NavPlanFilterIcon" Foreground="{ThemeResource IconForegroundBrush}" />
                    </Viewbox>
                </Button>
            </Grid>
            <Grid x:Name="NavDiaryHost" Grid.Column="3">
                <Button x:Name="NavDiary" Style="{StaticResource NovaraNavButtonStyle}" Click="NavButton_Click" Tag="Diary" IsTabStop="False" ToolTipService.ToolTip="Ctrl+4">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Path x:Name="NavDiaryPath" Width="20" Height="20" Stretch="Uniform"
                              Fill="{ThemeResource IconForegroundBrush}" />
                        <TextBlock x:Name="NavDiaryText" Text="记录" Tag="Nav_Tab_Diary" VerticalAlignment="Center" FontSize="14" />
                    </StackPanel>
                </Button>
                <Button x:Name="NavDiaryFilterButton"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Margin="0,0,10,0"
                        Width="28" Height="28"
                        Background="Transparent"
                        BorderThickness="0"
                        CornerRadius="8"
                        Padding="0"
                        Visibility="Collapsed"
                        IsTabStop="False"
                        Click="NavDiaryFilterButton_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Grid x:Name="Root" Background="{TemplateBinding Background}" CornerRadius="{TemplateBinding CornerRadius}">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Root" Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="#33FFFFFF" />
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Root" Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="#59FFFFFF" />
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <ContentPresenter Content="{TemplateBinding Content}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Grid>
                        </ControlTemplate>
                    </Button.Template>
                    <Viewbox Width="14" Height="14" Stretch="Uniform">
                        <PathIcon x:Name="NavDiaryFilterIcon" Foreground="{ThemeResource IconForegroundBrush}" />
                    </Viewbox>
                </Button>
            </Grid>
        </Grid>

        
        <Grid x:Name="ChromeScrim" Grid.Row="0" Grid.RowSpan="2"
              Background="#2E000000" IsHitTestVisible="False" Opacity="0" Visibility="Collapsed" />

        <Grid Grid.Row="2">
            <Frame x:Name="RootFrame" />

            <Grid x:Name="WelcomeOverlay" Visibility="Visible" Background="Transparent" Tapped="WelcomeOverlay_Tapped">
                <Grid.Resources>

                    <Storyboard x:Name="WelcomeEntryAnimation">
                        <DoubleAnimation Storyboard.TargetName="WelcomeLogo" Storyboard.TargetProperty="Opacity"
                                         From="0" To="1" Duration="0:0:1.8" BeginTime="0:0:0.3">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut" />
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetName="WelcomeTitleTransform" Storyboard.TargetProperty="X"
                                         From="-100" To="0" Duration="0:0:1.5" BeginTime="0:0:0.7">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut" />
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetName="WelcomeTitleGroup" Storyboard.TargetProperty="Opacity"
                                         From="0" To="1" Duration="0:0:1.2" BeginTime="0:0:0.7">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut" />
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetName="WelcomeSubtitleTransform" Storyboard.TargetProperty="X"
                                         From="100" To="0" Duration="0:0:1.5" BeginTime="0:0:1.1">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut" />
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetName="WelcomeSubtitle" Storyboard.TargetProperty="Opacity"
                                         From="0" To="1" Duration="0:0:1.2" BeginTime="0:0:1.1">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut" />
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetName="WelcomeHint" Storyboard.TargetProperty="Opacity"
                                         From="0" To="0.5" Duration="0:0:1.6" BeginTime="0:0:1.7">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut" />
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>

                    <Storyboard x:Name="WelcomeHintBreathing" RepeatBehavior="Forever" AutoReverse="True">
                        <DoubleAnimation Storyboard.TargetName="WelcomeHint" Storyboard.TargetProperty="Opacity"
                                         From="0.3" To="0.7" Duration="0:0:3.0">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseInOut" />
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </Grid.Resources>

                <!-- Logo -->
                <Image x:Name="WelcomeLogo"
                       Source="ms-appx:///Assets/Novara-R.png"
                       Width="80" Height="80"
                       Stretch="Uniform"
                       Opacity="0"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center">
                    <Image.RenderTransform>
                        <TranslateTransform x:Name="WelcomeLogoTransform" Y="0" />
                    </Image.RenderTransform>
                </Image>

                <Grid x:Name="WelcomeTitleGroup"
                      Opacity="0"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center">
                    <Grid.RenderTransform>
                        <TranslateTransform x:Name="WelcomeTitleTransform" X="0" Y="0" />
                    </Grid.RenderTransform>
                    <TextBlock x:Name="WelcomeTitleShadowText"
                               Text="Novara"
                               FontFamily="Century Gothic"
                               FontSize="80"
                               Foreground="{ThemeResource AppTextPrimaryBrush}"
                               Opacity="0.06"
                               Margin="2,2,0,0" />
                    <TextBlock x:Name="WelcomeTitleText"
                               Text="Novara"
                               FontFamily="Century Gothic"
                               FontSize="80"
                               Foreground="{ThemeResource AppTextPrimaryBrush}" />
                </Grid>

                <TextBlock x:Name="WelcomeSubtitle"
                           Text="本地保管每一份私密备忘" Tag="Welcome_Subtitle"
                           FontFamily="Segoe UI Variable Text, Segoe UI"
                           FontSize="34"
                           FontWeight="ExtraLight"
                           Foreground="{ThemeResource AppTextSecondaryBrush}"
                           CharacterSpacing="850"
                           Opacity="0"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">
                    <TextBlock.RenderTransform>
                        <TranslateTransform x:Name="WelcomeSubtitleTransform" X="0" Y="0" />
                    </TextBlock.RenderTransform>
                </TextBlock>

                <TextBlock x:Name="WelcomeHint"
                           Text="点击任意处开始" Tag="Welcome_Hint_ClickToStart"
                           FontFamily="Segoe UI Variable Text, Segoe UI"
                           FontSize="14"
                           Foreground="{ThemeResource AppTextTertiaryBrush}"
                           Opacity="0"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">
                    <TextBlock.RenderTransform>
                        <TranslateTransform x:Name="WelcomeHintTransform" Y="0" />
                    </TextBlock.RenderTransform>
                </TextBlock>
            </Grid>
        </Grid>

        <Grid x:Name="BottomToolbarPanel" Grid.Row="2"
              HorizontalAlignment="Right" VerticalAlignment="Bottom"
              Margin="0,0,18,20" Width="68" Height="48">
            <!-- Transparent hover zone covering the full un-collapsed button position (56x32 centred
                 over the taller bar). PointerEntered/Exited drive the melt expand / auto collapse. -->
            <Border x:Name="MenuHoverZone" Background="Transparent"
                    PointerEntered="MenuHoverZone_PointerEntered"
                    PointerExited="MenuHoverZone_PointerExited" />
            <!-- The button itself carries both states: collapsed = 8x48 vertical bar, expanded = 56x32
                 hamburger. The melt animates width/height/corner/icon in OnMenuRendering. -->
            <Button x:Name="MenuButton"
                    Style="{StaticResource NovaraPrimaryButtonStyle}"
                    Click="MenuButton_Click"
                    Width="8" Height="48" Padding="0"
                    CornerRadius="4"
                    HorizontalAlignment="Right" VerticalAlignment="Center"
                    IsTabStop="False"
                    PointerEntered="MenuButton_PointerEntered"
                    PointerExited="MenuButton_PointerExited">
                
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Grid Background="{TemplateBinding Background}" CornerRadius="{TemplateBinding CornerRadius}">
                            <ContentPresenter Content="{TemplateBinding Content}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                    </ControlTemplate>
                </Button.Template>
                <Button.RenderTransform>
                    <TranslateTransform x:Name="MenuButtonTranslate" X="6" />
                </Button.RenderTransform>
                <Path x:Name="MenuPathIcon"
                      Width="18" Height="18"
                      Stretch="Uniform"
                      Fill="#FFFFFFFF"
                      Opacity="0" />
            </Button>
        </Grid>

        <Grid x:Name="ThemeRestartOverlay" Visibility="Collapsed" Grid.RowSpan="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid x:Name="ThemeRestartScrim" Background="#2E000000" Tapped="ThemeRestartScrim_Tapped" />
            <Border x:Name="ThemeRestartDialog" HorizontalAlignment="Center" VerticalAlignment="Center" Width="420" CornerRadius="24" Background="{ThemeResource AppDialogBgBrush}" BorderBrush="{ThemeResource AppDialogBorderBrush}" BorderThickness="1" Opacity="0">
                <Border.RenderTransform>
                    <CompositeTransform x:Name="ThemeRestartDialogTransform" ScaleX="0.92" ScaleY="0.92" TranslateY="20" />
                </Border.RenderTransform>
                <Grid Padding="28,24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <TextBlock Text="检测到系统主题变更" Tag="Theme_Change_Detected"
                                   HorizontalAlignment="Left" VerticalAlignment="Center"
                                   FontSize="18" FontWeight="SemiBold"
                                   Foreground="{ThemeResource AppTextPrimaryBrush}" />
                        <Button HorizontalAlignment="Right" VerticalAlignment="Center"
                                Width="28" Height="28"
                                Style="{StaticResource NovaraIconButtonStyle}"
                                Click="ThemeRestartClose_Click">
                            <Path Data="M278.528 278.528L745.472 745.472M745.472 278.528L278.528 745.472"
                                  Stroke="{ThemeResource IconForegroundBrush}"
                                  StrokeThickness="1.8" Stretch="Uniform" Width="16" Height="16" />
                        </Button>
                    </Grid>
                    <TextBlock Grid.Row="1"
                               Text="主题将在重启程序后生效，是否立即重启？" Tag="Theme_Restart_ConfirmTip"
                               FontSize="14"
                               Foreground="{ThemeResource AppTextSecondaryBrush}"
                               TextWrapping="Wrap"
                               Margin="0,20,0,0"
                               LineHeight="22" />
                    <Grid Grid.Row="2" Margin="0,20,0,0">
                        <Button HorizontalAlignment="Left" Width="160" Height="36"
                                Style="{StaticResource NovaraOutlineButtonStyle}"
                                CornerRadius="10" FontSize="14"
                                Click="ThemeRestartCancel_Click">
                            <TextBlock Text="取消" Tag="Common_Button_Cancel" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Button>
                        <Button HorizontalAlignment="Right" Width="160" Height="36"
                                Style="{StaticResource NovaraPrimaryButtonStyle}"
                                CornerRadius="10" FontSize="14" FontWeight="SemiBold"
                                Click="ThemeRestartConfirm_Click">
                            <TextBlock Text="确认重启" Tag="Theme_Restart_ConfirmButton" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Button>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <Frame x:Name="LockScreenFrame" Grid.RowSpan="4" Visibility="Collapsed" />

        <Grid x:Name="StoreCorruptOverlay" Visibility="Collapsed" Grid.RowSpan="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid x:Name="StoreCorruptScrim" Background="#2E000000" />
            <Border x:Name="StoreCorruptDialog" HorizontalAlignment="Center" VerticalAlignment="Center" Width="440" CornerRadius="24" Background="{ThemeResource AppDialogBgBrush}" BorderBrush="{ThemeResource AppDialogBorderBrush}" BorderThickness="1" Opacity="0">
                <Border.RenderTransform>
                    <CompositeTransform x:Name="StoreCorruptDialogTransform" ScaleX="0.92" ScaleY="0.92" TranslateY="20" />
                </Border.RenderTransform>
                <Grid Padding="28,24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="StoreCorruptTitleText" Grid.Row="0" Text="资料库文件校验失败" Tag="Store_Corrupt_Title"
                               HorizontalAlignment="Left" VerticalAlignment="Center"
                               FontSize="18" FontWeight="SemiBold"
                               Foreground="{ThemeResource AppTextPrimaryBrush}" />
                    <TextBlock x:Name="StoreCorruptDetailText" Grid.Row="1"
                               Text="资料库文件校验失败，文件已损坏或被篡改，程序将重建空白资料库，原有全部数据无法恢复" Tag="Store_Corrupt_Detail"
                               FontSize="14"
                               Foreground="{ThemeResource AppTextSecondaryBrush}"
                               TextWrapping="Wrap"
                               Margin="0,20,0,0"
                               LineHeight="22" />
                    <Grid Grid.Row="2" Margin="0,24,0,0">
                        <Button x:Name="StoreCorruptConfirmButton" HorizontalAlignment="Right" Width="160" Height="36"
                                Background="#CCFF4545" Foreground="#FFFFFFFF"
                                CornerRadius="10" FontSize="14" FontWeight="SemiBold"
                                BorderThickness="0"
                                Click="StoreCorruptConfirm_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Grid x:Name="RootGrid" Background="{TemplateBinding Background}" CornerRadius="{TemplateBinding CornerRadius}">
                                        <VisualStateManager.VisualStateGroups>
                                            <VisualStateGroup x:Name="CommonStates">
                                                <VisualState x:Name="Normal" />
                                                <VisualState x:Name="PointerOver">
                                                    <Storyboard>
                                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="Background">
                                                            <DiscreteObjectKeyFrame KeyTime="0" Value="#FFFF4545" />
                                                        </ObjectAnimationUsingKeyFrames>
                                                    </Storyboard>
                                                </VisualState>
                                                <VisualState x:Name="Pressed">
                                                    <Storyboard>
                                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="Background">
                                                            <DiscreteObjectKeyFrame KeyTime="0" Value="#CC3A3A" />
                                                        </ObjectAnimationUsingKeyFrames>
                                                    </Storyboard>
                                                </VisualState>
                                                <VisualState x:Name="Disabled" />
                                            </VisualStateGroup>
                                        </VisualStateManager.VisualStateGroups>
                                        <ContentPresenter Content="{TemplateBinding Content}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Grid>
                                </ControlTemplate>
                            </Button.Template>
                            <TextBlock x:Name="StoreCorruptConfirmText" Text="重建资料库" Tag="Store_Corrupt_RebuildButton" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Button>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <Grid x:Name="SaveFailedOverlay" Visibility="Collapsed" Grid.RowSpan="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid x:Name="SaveFailedScrim" Background="#2E000000" />
            <Border x:Name="SaveFailedDialog" HorizontalAlignment="Center" VerticalAlignment="Center" Width="440" CornerRadius="24" Background="{ThemeResource AppDialogBgBrush}" BorderBrush="{ThemeResource AppDialogBorderBrush}" BorderThickness="1" Opacity="0">
                <Border.RenderTransform>
                    <CompositeTransform x:Name="SaveFailedDialogTransform" ScaleX="0.92" ScaleY="0.92" TranslateY="20" />
                </Border.RenderTransform>
                <Grid Padding="28,24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="数据保存失败" Tag="Store_SaveFail_Title"
                               HorizontalAlignment="Left" VerticalAlignment="Center"
                               FontSize="18" FontWeight="SemiBold"
                               Foreground="{ThemeResource AppTextPrimaryBrush}" />
                    <TextBlock Grid.Row="1"
                               Text="数据保存失败，请检查磁盘剩余空间、文件夹读写权限" Tag="Store_SaveFail_Detail"
                               FontSize="14"
                               Foreground="{ThemeResource AppTextSecondaryBrush}"
                               TextWrapping="Wrap"
                               Margin="0,20,0,0"
                               LineHeight="22" />
                    <Grid Grid.Row="2" Margin="0,24,0,0">
                        <Button HorizontalAlignment="Right" Width="120" Height="36"
                                Style="{StaticResource NovaraPrimaryButtonStyle}"
                                CornerRadius="10" FontSize="14"
                                Click="SaveFailedConfirm_Click">
                            <TextBlock Text="知道了" Tag="Common_Button_GotIt" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Button>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <!-- 4.7: v1-CBC -> v2-GCM migration confirmation (shown once after unlock when NeedsFormatMigration) -->
        <Grid x:Name="MigrateFormatOverlay" Visibility="Collapsed" Grid.RowSpan="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid x:Name="MigrateFormatScrim" Background="#2E000000" />
            <Border x:Name="MigrateFormatDialog" HorizontalAlignment="Center" VerticalAlignment="Center" Width="480" CornerRadius="24" Background="{ThemeResource AppDialogBgBrush}" BorderBrush="{ThemeResource AppDialogBorderBrush}" BorderThickness="1" Opacity="0">
                <Border.RenderTransform>
                    <CompositeTransform x:Name="MigrateFormatDialogTransform" ScaleX="0.92" ScaleY="0.92" TranslateY="20" />
                </Border.RenderTransform>
                <Grid Padding="28,24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="MigrateFormatTitle" Grid.Row="0" Text="升级数据加密格式" Tag="Security_Migrate_Title"
                               HorizontalAlignment="Left" VerticalAlignment="Center"
                               FontSize="18" FontWeight="SemiBold"
                               Foreground="{ThemeResource AppTextPrimaryBrush}" />
                    <TextBlock x:Name="MigrateFormatMessage" Grid.Row="1"
                               Text="本次将升级至全新 AES-256-GCM 加密格式" Tag="Security_Migrate_Body"
                               FontSize="14"
                               Foreground="{ThemeResource AppTextSecondaryBrush}"
                               TextWrapping="Wrap"
                               Margin="0,20,0,0"
                               LineHeight="22" />
                    <Grid Grid.Row="2" Margin="0,24,0,0">
                        <Button x:Name="MigrateLaterButton" HorizontalAlignment="Left" Width="160" Height="36"
                                Style="{StaticResource NovaraOutlineButtonStyle}"
                                CornerRadius="10" FontSize="14"
                                Click="MigrateLater_Click">
                            <TextBlock Text="暂不升级" Tag="Security_Migrate_Later" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Button>
                        <Button x:Name="MigrateConfirmButton" HorizontalAlignment="Right" Width="180" Height="36"
                                Style="{StaticResource NovaraPrimaryButtonStyle}"
                                CornerRadius="10" FontSize="14" FontWeight="SemiBold"
                                Click="MigrateConfirm_Click">
                            <TextBlock x:Name="MigrateConfirmText" Text="确认升级" Tag="Security_Migrate_Confirm" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Button>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        
        <Grid x:Name="McpAuthorizeOverlay" Visibility="Collapsed" Grid.RowSpan="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid x:Name="McpAuthorizeScrim" Background="#2E000000" />
            <Border x:Name="McpAuthorizeDialog" HorizontalAlignment="Center" VerticalAlignment="Center" Width="460" CornerRadius="24" Background="{ThemeResource AppDialogBgBrush}" BorderBrush="{ThemeResource AppDialogBorderBrush}" BorderThickness="1" Opacity="0">
                <Border.RenderTransform>
                    <CompositeTransform x:Name="McpAuthorizeDialogTransform" ScaleX="0.92" ScaleY="0.92" TranslateY="20" />
                </Border.RenderTransform>
                <Grid Padding="28,24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" x:Name="McpAuthorizeTitle"
                               Text="MCP 访问授权" Tag="Mcp_Authorize_Title"
                               HorizontalAlignment="Left" VerticalAlignment="Center"
                               FontSize="18" FontWeight="SemiBold"
                               Foreground="{ThemeResource AppTextPrimaryBrush}" />
                    <StackPanel Grid.Row="1" Margin="0,20,0,0" Spacing="8">
                        <TextBlock x:Name="McpAuthorizeMessage"
                                   Text="检测到以下进程尝试通过 MCP 访问 Novara，是否允许？" Tag="Mcp_Authorize_Message"
                                   FontSize="14" Foreground="{ThemeResource AppTextSecondaryBrush}"
                                   TextWrapping="Wrap" LineHeight="22" />
                        <TextBlock x:Name="McpAuthorizePath"
                                   FontSize="13" Foreground="{ThemeResource AppTextPrimaryBrush}"
                                   TextWrapping="Wrap" TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                    <Grid Grid.Row="2" Margin="0,24,0,0">
                        <Button HorizontalAlignment="Left" Width="160" Height="36"
                                Style="{StaticResource NovaraOutlineButtonStyle}"
                                CornerRadius="10" FontSize="14"
                                Click="McpAuthorizeDeny_Click">
                            <TextBlock Text="拒绝" Tag="Mcp_Authorize_Deny" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Button>
                        <Button HorizontalAlignment="Right" Width="160" Height="36"
                                Style="{StaticResource NovaraPrimaryButtonStyle}"
                                CornerRadius="10" FontSize="14" FontWeight="SemiBold"
                                Click="McpAuthorizeAllow_Click">
                            <TextBlock Text="允许" Tag="Mcp_Authorize_Allow" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Button>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <Grid x:Name="ToastHost" Grid.RowSpan="3" IsHitTestVisible="False" />
    </Grid>
</Window>