# Novara 隐私政策

<p align="center"><a href="PRIVACY.md">English</a> · <strong>简体中文</strong></p>

---

**生效日期：** 2026-08-14
**最近更新：** 2026-08-17
**适用范围：** Novara 4.0（以及下述行为在更早版本中已存在的情形，包括 Novara 3.0 与 2.0）

> 本政策描述的是 Novara **桌面应用**（Windows 版）。官方网站（novara.xin）与 GitHub 仓库为独立资产；本文档仅针对你安装并运行在本机上的软件。

---

## TL;DR

- **Novara 100% 本地化。** 你的数据保存在你自己的电脑上，除非你主动让它离开，否则绝不会离开。
- **我们不收集任何东西。** 没有账号、没有遥测、没有使用统计、没有广告、没有设备指纹。
- **完全可离线使用。** 整个应用里唯一的网络活动，是可选的手动触发的 API Key 连通性测试。
- **加密是可选的，且默认关闭。** 请仔细阅读第 4 节，确切了解哪些内容受保护、哪些不受保护。

---

## 1. Novara 是什么

Novara 是一款面向 Windows 的本地优先个人知识管理工具，使用 C# 与 WinUI 3 构建。它将备忘、文件路径书签、待办清单、桌面便签与富文本日记整合进一个完全存储在你本机上的数据库文件。

Novara **没有云后端**、没有账号体系，也不存在任何由 Novara 项目运营、可能接收你数据的服务器。应用被设计为在完全无网络连接的情况下也能完整使用。

## 2. 我们收集什么：什么都不收集

Novara 应用**不会**收集、传输，也不会存储到任何远程服务器：

- 你的笔记、密码、日记、待办项或你创建的任何内容；
- 你的身份、姓名或邮箱地址（无需注册）；
- 使用统计、功能使用日志或行为分析；
- 设备信息、硬件标识符或崩溃报告（见第 7 节）；
- 你的 IP 地址或网络活动。

由于一切都在你自己的机器上处理与存储，开发者**在技术上无法查看、读取或访问你的数据**。

## 3. 你的数据存放在哪里

Novara 的所有数据都写入一个本地目录：

```
%LocalAppData%\Novara\
```

> **开发构建：** 测试/开发版改用 `%LocalAppData%\Novara-Dev\`，从而避免测试数据与你已安装的正式版混在一起。本节其余内容对两者均适用。

该目录中的文件包括：

| 文件 | 用途 |
|------|---------|
| `data.novadb` | 你的整个数据库（备忘、路径、待办、便签、日记、设置）——单一文件 |
| `security.dat` | 仅保存你锁定密码的加盐哈希（绝不保存密码明文）；见第 4 节 |
| `lockout.dat` | 多次输错密码后的锁定状态（失败次数与锁定期限） |
| `language.dat` | 你选择的界面语言的纯文本提示（用于在解锁前以正确语言显示锁屏） |
| `stickies.json` | 用于主程序与桌面便签辅助进程之间同步的数据 |
| `backups\` | 内置备份功能生成的滚动本地快照（4.0+） |
| `logs\` | 本地日志文件（见第 7 节） |

该目录中的任何内容都不会被上传。删除该目录（或使用应用内「重置」功能）即会删除你的数据。

## 4. 加密——请仔细阅读

Novara 的加密是**可选的，且默认关闭**。

- **默认状态：** 数据库以明文 JSON 文件形式存储在你本地磁盘上。它受 Windows 用户账户的文件权限保护，但**在你开启隐私锁之前并未加密**。
- **开启隐私锁后：** 整个数据库使用 **AES-256-GCM**（认证加密）加密。加密密钥由你的密码经 **PBKDF2-SHA256（100,000 次迭代）** 与每用户随机盐派生。GCM 还能检测篡改：如果加密文件被修改，Novara 会报告其已损坏，而不会静默地读错。
- **密码：** 锁定密码长度为 **6 到 64 个字符**。本地只保存密码的加盐 **SHA-256 哈希**——明文密码绝不会写入磁盘。
- **无后门、无找回：** 刻意不提供任何找回遗忘密码的途径。如果你忘了密码，唯一的办法是清空数据库、重新开始。
- **防暴力破解：** 连续输错 5 次密码后，解锁将被锁定 30 分钟。失败次数与锁定期限在重启后依然保留，且自 4.0 起锁定使用单调时钟，以抵御通过修改系统时间来绕过的尝试。
- **升级路径：** Novara 2.0 创建的数据库使用较旧的 AES-CBC 格式。Novara 3.0/4.0 可读取它们，并在一次确认后迁移到较新的 GCM 格式。

**加密能保护什么、不能保护什么：** 完整威胁模型见安全政策（SECURITY.md）。简言之，加密保护的是你「静态」的数据（应用锁定或关闭时磁盘上的文件）。它无法保护已解锁在内存中的数据，也无法防御恶意软件、键盘记录器、屏幕截图，或对你已解锁设备的物理接触。

## 5. 网络行为

Novara 被设计为**可完全离线使用**，不会回连服务器（phone home）。

- **无自动更新：** Novara 不会自行检查、下载或安装更新。
- **无遥测或分析：** 应用绝不会联系任何由 Novara 项目运营的服务器。
- **唯一一项网络功能：** 备忘管理中的 **API Key 连通性测试**。

### API Key 连通性测试（唯一的网络调用）

部分备忘条目类型为「API Key」。如果你右键此类条目并选择**检测连通性**，Novara 会：

1. 向你为该条目**自行配置的端点 URL** 发送一个请求（通常是模型列表路径如 `/v1/models` 的 `GET` 请求），并在请求中携带该条目的 API Key；
2. 将端点域名与内置的常见供应商列表（OpenAI、OpenRouter、Anthropic、Google Gemini、Azure OpenAI、Groq、Together、Perplexity、智谱、通义、DeepSeek、Moonshot 等）进行匹配，以确定鉴权方式与路径；未知端点则回退到通用的 OpenAI 兼容探测；
3. 在本地显示结果（成功 / 鉴权失败 / 无模型端点 / 网络错误）。

这是**唯一**将你的数据发送到网络的时刻，并且它：

- **仅由你的显式操作触发**；
- **仅发送到你自行配置的第三方端点**——绝不会发送给 Novara 项目；
- 不会被 Novara 保存、记录或转发到任何其他地方。

如果你不使用此功能，Novara 会产生 **零** 网络请求。

## 6. Novara 会访问你设备上的什么

Novara 刻意将接触范围降到最小：

- **它自己的数据目录**（`%LocalAppData%\Novara\`，开发构建为 `Novara-Dev`）——只在这里读写。
- **你自行选择的文件/文件夹**——「路径备份」功能允许你登记文件或文件夹路径。Novara 仅检查这些特定路径是否存在（绿/红边框），并在你点击时用文件资源管理器打开它们。它**不会**扫描整个磁盘，也不会读取无关文件。
- **文件/文件夹选择器**——在你创建或编辑路径条目时，选择器让你自行选择文件或文件夹。
- **Windows 注册表（仅当前用户，`HKCU`）**——Novara 为你开启的可选功能写入少量用户级注册表项：
  - `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 下的「开机自启」键；
  - `HKCU\Software\Classes\` 下的右键菜单项（桌面背景、文件/文件夹右键菜单）。
  这些在你关闭相应功能或卸载应用时会被移除。

Novara **不会**请求或使用：摄像头、麦克风、位置、通讯录，或除上述显式连通性测试之外的任何网络权限。

## 7. 日志与诊断

- 日志**仅保存在本地**，绝不会发送给开发者或任何第三方。
- 自 4.0 起，崩溃日志可能写入 `%LocalAppData%\Novara\logs\` 下的本地文件（如 `crash-*.txt`），以便日后在你愿意时用于诊断。日志中的敏感值（如 API Key）会被脱敏。
- 桌面便签辅助进程会在 `%LocalAppData%\Novara\logs\` 下写入自己的日志文件（`sticknotehost_debug.txt`）。
- **不存在自动诊断上报**。若你联系支持，可自行选择附上日志文件——未经你的操作，任何内容都不会被传输。

## 8. 第三方组件

Novara 基于标准 Microsoft 技术与若干开源库构建：

| 组件 | 作用 | 数据说明 |
|------|------|---------|
| WinUI 3 / Windows App SDK 1.6 | UI 框架 | 本地；Novara 不做数据收集 |
| .NET 8 | 运行时 | 本地 |
| WebView2（Edge Chromium） | 承载富文本日记编辑器 | 微软组件；可能保留其自身的本地运行时数据，其处理遵循微软的隐私声明。日记编辑器本身**离线**运行（编辑器引擎为内置，无 CDN）。 |
| H.NotifyIcon | 系统托盘图标 | 本地 |
| Tiptap | 日记编辑器引擎 | 作为本地脚本（`tiptap.bundle.js`）打包，完全离线运行 |
| AngleSharp | 日记编辑器的 HTML 净化 | 本地；用于剥离粘贴内容中的不安全 HTML |

Novara 不嵌入任何广告 SDK、分析 SDK 或第三方追踪代码。

## 9. 数据共享

Novara **不会共享、出售、出租或传输**你的数据给任何第三方。不存在任何涉及你数据的商业模式——没有广告、没有分析、没有联盟追踪。

唯一可能离开你机器的数据，是你自己发送的数据：

- API Key 连通性测试（发送到你配置的端点），以及
- 你主动导出或备份到你选择位置的文件。

## 10. 你对数据的控制权

你始终拥有完全的控制权：

- **导出（原生格式）**——导出带有完整性（MD5）头的完整明文备份（`.novabak`）。*注意：导出的文件未加密*——请妥善保管。路径备份条目默认排除（可选包含），以便迁移到新机器。
- **Markdown 导出**——将数据导出为只读 Markdown 摘要，可选择含图片或不含图片（4.0+）。仅供阅读/分享，无法重新导入。
- **备份快照**——内置备份保留最多 10 份滚动本地快照，可随时恢复（4.0+）。
- **导入 / 恢复**——导入备份；校验、归一化与回滚可防止损坏或重复数据。
- **回收站**——删除的卡片进入回收站，7 天内可恢复，之后在下一次启动时被永久清除。
- **重置 / 删除**——应用内「重置」会清空数据库并重新开始。
- **隐私锁**——你随时可以在设置中开启、更改或关闭加密。关闭后数据库会重新以明文保存。

## 11. 我们永远不会做的事

Novara 项目在设计中承诺如下：

- 我们**永不**运营收集你数据的云服务器。
- 我们**永不**加入遥测、分析或广告。
- 我们**永不**加入后门、密码找回后门，或任何远程解锁/窃取你数据的机制。
- 我们**永不**在未经你显式操作的情况下自动更新、自动上传或以其他方式移动你的数据。

## 12. 儿童隐私

Novara 不收集任何人的任何个人信息，包括儿童。由于应用完全本地化、不收集任何信息，因此不适用特殊的儿童隐私条款。

## 13. 本政策的变更

随着应用演进（例如新增功能），本政策可能更新。更新将随新版本一同发布。更新后的政策自其生效日期起适用，不会影响或改变已存储在你设备上的数据。

## 14. 联系方式

如对隐私有疑问或请求，请联系：

- **邮箱：** owner@novara.xin
- **网站：** https://novara.xin
- **GitHub：** https://github.com/Novara-owner/Novara-Vault
