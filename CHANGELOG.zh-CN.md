# 更新日志

<p align="center"><a href="CHANGELOG.md">English</a> · <strong>简体中文</strong></p>

Novara 的所有重要变更均记录于此。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [6.1.0] - 2026-09-06

### 修复

- **备忘页：清空备忘数据后新建分组「看起来失败」。** 分组实际已创建并落库，但工作区时代的「空分组隐藏」过滤规则无差别生效，把新建的空分组卡片立刻折叠（且每次重新加载都保持隐藏），于是看起来毫无反应。现在无工作区过滤时空分组恒可见——隐藏规则仅在某个工作区激活时生效，这才是它的本意。
- 工作区激活时新建空分组仍按设计隐藏（分组可见性由其条目推导）；此时 Toast 改为明确说明，而不是笼统的「已创建」。

## [6.0.0] - 2026-09-06

### 新增

- **动效设计系统** —— 全局统一的令牌化动效层：导航「藏光」辉光、切页过渡、弹窗景深与逐行入场、卡片入场、按压反馈。时长与缓动曲线出自单一来源（`Services/Motion.cs`），动效始终一致，不再各自为政。
- **工作区（Workspaces）** —— 横跨五类数据的轻量虚拟分组。把卡片划入某个空间，切换空间即同时重塑所有列表；数据库本身保持扁平，数据不挪动。
- **快速捕获（Quick Capture）** —— 全局热键随时唤出置顶输入框，一键分派为备忘 / 待办 / 便签。
- **网络活动指示器** —— 标题栏常显「仅本地」状态，发起 API 检测等用户主动的网络调用时短暂显示目标端点，出网流量不再无声。
- **首启导览** —— 首次启动的四页产品导览，全部由真实 UI 复刻：菜单、导航、桌面便签演示与隐私锁。

### 变更

- 计划页与记录页取消置顶后卡片原地停留（与备忘页口径一致），不再掉回时间序位置；全程序右键菜单统一「置顶」在「星标」之前。

### 修复

- 五轮全域核验（N1–N5）累计修复约 250 项。重点：加密状态转换（开启 / 关闭 / 迁移 / 改密）全面事务化——不再存在「磁盘已是明文而内存自认加密」的窗口；快照恢复两阶段化并支持回滚；IPC pending 文件原子写入；回收站分批渲染不再因切页重入产生重复卡片；标题输入不再吞掉输入法候选词确认。

### 安全

- MCP 加固：畸形 JSON 按规范回 `-32700` 错误（不再让客户端挂到超时）；`read_item` 参数校验前置于权限门；审计日志字段限长并掩码凭据；聊天响应体上限 32 MB。
- CSV 公式注入防护往返对称：以引号开头的值经导出 / 导入后原样保留。
- 快照文件名在删除 / 恢复前校验，阻断越出备份目录的路径穿越。
- MCP 握手的 `serverInfo.version` 改为上报真实程序集版本。

## [5.3.0] - 2026-08-31

### 新增

- **TOTP 两步验证** —— 向邮箱 / 账户 / 网站 / WiFi 条目粘贴 `otpauth://` URI 或 Base32 密钥，卡片即显示实时 6 位动态码、剩余秒数、倒计时进度条与一键复制。
- **Agent 权限中心** —— 每个已授权的 MCP 客户端拥有独立的 20 位权限矩阵（读 / 建 / 改 / 删 × 五类数据）。新客户端除备忘外默认只读；删除另受全局总闸约束。越权尝试记入审计日志。
- **数据库健康检查** —— 数据概览卡新增加密状态、最近备份、快照份数、完整性与孤儿引用指标。
- **随机生成器** —— 条目对话框内直接生成随机密码 / UUID / Token。
- **命令面板** —— Ctrl+K 输入 `>` 前缀即切换为命令模式：新建条目、打开页面、立即锁定。
- **备注一键复制** —— 备注字段与其他敏感字段一样获得行尾复制按钮。

### 变更

- **密钥派生强化（格式 v3）** —— PBKDF2-SHA256 迭代数由 100,000 提升至 3,000,000（参考机解锁约 340 ms）。v2 库经一次性选择权弹窗迁移；锁屏密码哈希同样升级为 PBKDF2。**升级到 v3 的数据库无法被 5.2.0 及更早版本打开。**
- **桌面便签宿主以完整自包含包随发**，位于 `Host\` 子目录——旧的裸 exe 部署在全新机器上即死（事件 1023）。
- 各页软删除确认弹窗统一；垃圾桶 / 锁 / 命令行图标刷新。

### 安全

- AngleSharp 升级至 1.5.0（CVE-2026-54570）。
- 公开工程文件中移除机器本地的 `AppxMSBuildToolsPath` 属性。

## [5.2.0] - 2026-08-29

### 新增

- **加密备份导出 / 导入（`.novaenc`）** —— 自包含 v4 容器（密码 → KDF → AES-256-GCM，头部参与 AAD），使用独立备份密码并带强度提示。明文导出时明确警告文件包含敏感数据。

### 修复

- 根治同步锁定死循环（会话锁判据）与隐私门禁启动流的回归。

## [5.1.0] - 2026-08-29

### 新增

- **MCP 审计日志** —— 每次代理调用均落本地日志：何进程、何时、何工具、何对象、结果如何（含被拒绝的尝试）。可在 MCP 设置卡片查看。
- **自动锁定与立即锁定** —— 手动（Ctrl+Shift+L）、空闲计时（5 / 10 / 30 / 60 分钟）或 Windows 锁屏时联动锁定。
- **SHA-256 导出完整性头** —— 明文导出升级校验头；旧 MD5 头文件经双头探测仍可导入。

### 修复

- N6 轮核验约 20 项修复，含部分机器上的启动闪退（`0xc000027b`）。

## [5.0.0] - 2026-08-25

### 新增

- **MCP Agent 接口** —— 原生 MCP 服务器（`NovaraMCP.exe`），提供 14 个工具，覆盖五类数据（`memo` / `todo` / `note` / `diary` / `path`）：`create_*`、`update_*`、`delete_item`、`list_items`、`read_item`、`search_items`。
- **记录页** —— Markdown 文档与 HTML 日记共处一页：源码 / 预览分屏编辑器（WebView2 + markdown-it）、Markdown 导入、筛选栏、格式徽标。
- **API 中转站探针** —— 八项检测（身份真伪、能力基准、格式遵从、token 计费、工具调用、隐藏注入、响应投毒、长上下文截断），产出加权风险结论；随机化 nonce 提示 + 本地可更新 `ProbeDataSet.json`。
- **API 状态诊断** —— 第二个检测入口，推断有无余额、读取元数据、测量延迟 / TTFT。
- **8 种备忘条目类型** —— 新增银行卡、WiFi、证件。
- **CSV 导入/导出** —— 自动识别 Novara、KeePass、Bitwarden 三种格式。
- **PDF / HTML 合集导出** —— 将全部记录导出为可打印的 HTML 或 PDF 合集。
- **索引化 + 模糊全局搜索** —— Ctrl+K 更快，支持子序列模糊匹配与标题优先排序。
- **Windows Hello 解锁** —— 隐私锁的生物识别 / PIN 解锁。
- **滚动自动备份** —— 最多 10 份可恢复的本地快照。

### 变更

- **「日记」标签页更名为「记录」** —— 长文本页现在同时承载 HTML 日记与 Markdown 文档。

### 安全

- **MCP 安全模型** —— 默认关闭；令牌鉴权（固定时间比较）；数据库解锁门控；每进程授权白名单；删除独立权限；敏感字段脱敏 + 防改名绕过。

## [4.0.0] - 2026-08-18

### 新增

- 四页卡片拖拽排序，顺序持久化落盘。
- 桌面待办双向同步（桌面勾选写回主程序）。
- 日记 Markdown 导出（单篇，含 / 不含图片）。
- 数据概览面板（条目、分组、待办完成率、存储占用）。
- 快捷键（Ctrl+1~4 切标签、Ctrl+, 设置、Ctrl+Shift+Backspace 回收站）。
- 导出下拉（Markdown 汇总 / 原生备份）。

### 变更

- API 连通检测全面升级：厂商识别、协议矩阵、错误码精化、详情弹窗。
- 统一品牌按钮体系，按钮 / 弹窗 / 下拉框 / 复选框全面品牌美化。

### 安全

- 30 分钟锁定改用单调时钟（抵御系统时间回拨）。

### 工程

- 大列表分批渲染、崩溃日志本地落盘（脱敏）、滚动备份、写盘去抖、单元测试工程（`Novara.Core` + xUnit）。

## [3.0.0] - 2026-08-12

### 新增

- 桌面便签（StickNoteHost），支持拖动缩放、锁定置顶、主题 / 语言同步、桌面编辑。
- 全局搜索（Ctrl+K），点击直达 + 品牌色脉冲。
- 回收站，7 天自动清理。
- 五种界面语言（简体中文 / 繁體中文 / English / 한국어 / 日本語）+ 跟随系统。
- 系统提醒（桌面倒计时卡片）。
- 全局右键菜单（资源管理器添加路径 / 桌面打开 Novara）。
- 路径条目文件 / 文件夹选择器 + 路径有效性检测。
- 「网站」备忘类型；邮箱 / 账户条目必填校验。
- 导出配置（换机默认排除路径条目）。
- 富文本编辑器升级 Tiptap 2.27（WebView2）。

### 安全

- AES-256-GCM 认证加密（自动迁移旧版 AES-CBC 数据库）。
- 密码由 6 位数字 PIN 扩展为 6–64 位字符。
- 连续 5 次错误锁定 30 分钟，跨重启持久化。
- 双盐 SHA-256 密码哈希。

## [2.0.0] - 2026-08-07

### 新增

- 四大标签页（备忘、路径备份、计划、日记）+ 设置页 + 可选隐私锁。
- 加密存储（AES-256-CBC）、系统托盘、主题切换、双语界面。
