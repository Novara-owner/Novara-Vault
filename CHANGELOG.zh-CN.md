# 更新日志

<p align="center"><a href="CHANGELOG.md">English</a> · <strong>简体中文</strong></p>

Novara 的所有重要变更均记录于此。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [5.0.0] - 2026-08-25

### 新增

- **MCP Agent 接口** —— 原生 MCP 服务器（`NovaraMCP.exe`），提供 14 个工具，覆盖五类数据（`memo` / `todo` / `note` / `diary` / `path`）：`create_*`、`update_*`、`delete_item`、`list_items`、`read_item`、`search_items`。
- **记录页** —— Markdown 文档与 HTML 日记共处一页：源码 / 预览分屏编辑器（WebView2 + markdown-it）、Markdown 导入、筛选栏、格式徽标。
- **API 中转站探针** —— 八项检测（身份真伪、能力基准、格式遵从、token 计费、工具调用、隐藏注入、响应投毒、长上下文截断），产出加权风险结论；随机化 nonce 提示 + 本地可更新 `ProbeDataSet.json`。
- **API 状态诊断** —— 第二个检测入口，推断有无余额、读取元数据、测量延迟 / TTFT。
- **8 种备忘条目类型** —— 新增银行卡、WiFi、证件。
- **CSV 导入/导出** —— 自动识别 Novara、KeePass、Bitwarden 三种格式。
- **PDF / HTML 合集导出** —— 将全部记录导出为可打印的 HTML 或 PDF 合集。
- **索引化 + 模糊全局搜索** —— Ctrl+K 更快，支持子序列模糊匹配与标题优先排序。
- **Windows Hello 解锁** —— 隐私锁的生物识别 / PIN 解锁。
- **滚动自动备份** —— 最多 10 份可恢复的本地快照。

### 变更

- **「日记」标签页更名为「记录」** —— 长文本页现在同时承载 HTML 日记与 Markdown 文档。

### 安全

- **MCP 安全模型** —— 默认关闭；令牌鉴权（固定时间比较）；数据库解锁门控；每进程授权白名单；删除独立权限；敏感字段脱敏 + 防改名绕过。

## [4.0.0] - 2026-08-18

### 新增

- 四页卡片拖拽排序，顺序持久化落盘。
- 桌面待办双向同步（桌面勾选写回主程序）。
- 日记 Markdown 导出（单篇，含 / 不含图片）。
- 数据概览面板（条目、分组、待办完成率、存储占用）。
- 快捷键（Ctrl+1~4 切标签、Ctrl+, 设置、Ctrl+Shift+Backspace 回收站）。
- 导出下拉（Markdown 汇总 / 原生备份）。

### 变更

- API 连通检测全面升级：厂商识别、协议矩阵、错误码精化、详情弹窗。
- 统一品牌按钮体系，按钮 / 弹窗 / 下拉框 / 复选框全面品牌美化。

### 安全

- 30 分钟锁定改用单调时钟（抵御系统时间回拨）。

### 工程

- 大列表分批渲染、崩溃日志本地落盘（脱敏）、滚动备份、写盘去抖、单元测试工程（`Novara.Core` + xUnit）。

## [3.0.0] - 2026-08-12

### 新增

- 桌面便签（StickNoteHost），支持拖动缩放、锁定置顶、主题 / 语言同步、桌面编辑。
- 全局搜索（Ctrl+K），点击直达 + 品牌色脉冲。
- 回收站，7 天自动清理。
- 五种界面语言（简体中文 / 繁體中文 / English / 한국어 / 日本語）+ 跟随系统。
- 系统提醒（桌面倒计时卡片）。
- 全局右键菜单（资源管理器添加路径 / 桌面打开 Novara）。
- 路径条目文件 / 文件夹选择器 + 路径有效性检测。
- 「网站」备忘类型；邮箱 / 账户条目必填校验。
- 导出配置（换机默认排除路径条目）。
- 富文本编辑器升级 Tiptap 2.27（WebView2）。

### 安全

- AES-256-GCM 认证加密（自动迁移旧版 AES-CBC 数据库）。
- 密码由 6 位数字 PIN 扩展为 6–64 位字符。
- 连续 5 次错误锁定 30 分钟，跨重启持久化。
- 双盐 SHA-256 密码哈希。

## [2.0.0] - 2026-08-07

### 新增

- 四大标签页（备忘、路径备份、计划、日记）+ 设置页 + 可选隐私锁。
- 加密存储（AES-256-CBC）、系统托盘、主题切换、双语界面。
